- Remove mock dependency
- Remove six dependency
- Remove __future__ imports
- Replace super(ClassName, self) with super()
- Remove # -*- coding: utf-8 -*-
- Remove __ne__ from objects already defining a __eq__
- Replace errno checks by IOError subclasses
- Replace tempfile.mkdtemp() with TemporaryDirectory context manager
- Replace io.open()/codecs.open() by a plain open()
- Update tox
- Update trove classifiers
- Look at isinstance(..., bytes) and isinstance(..., str)
- Replace references to virtualenv with venv
- Fix shebangs
- Remove decode argument
- Drop u prefix from string literals
- Remove __unicode__ method
- Look for version_info, some uses might be outdated
- Look for ImportError, some might be outdated
- Add a line to the CHANGELOG
- Look for new-style class definition
- __nonzero__
- Replace ugettext with gettext
- Replace EnvironmentError, IOError, and WindowsError with OSError
- Make wheel no longer universal
- Use pyupgrade
- Replace os.devnull with subprocess.DEVNULL
- Look for NameError; might be outdated
- Look for ImportError; change to ModuleNotFound
- Bump Black's --target-version argument
- Look for "closing" from contextlib, may no longer be necessary
- Remove `def next()` in favor of `def __next__()`
